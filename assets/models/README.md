Modelo 3D conservado como recurso fuente, no cargado por la pagina actual:

- Archivo actual: `legal-model.3mf`
- Ruta del recurso: `./assets/models/legal-model.3mf`
- Estado actual: no se importa desde JavaScript para evitar peso y bloqueo visual.

La seccion interactiva usa ahora una imagen liviana desde `assets/images/`. Si mas adelante se necesita 3D web, conviene convertir el modelo a `.glb` / `.gltf` y optimizarlo antes de publicarlo.
